TODO

ship - movement script, map display script(works with move script to change destination), 

character - input script, movement script, interaction script, Ik handler(George's tool), animation handler(dont really need this tho, we can just feed the animator the values of movement to use in a blendtree), char values(level, health etc.), object placement scripts(gonna have to wait for ship to be done tho)
// note: make sure to keep in mind that this needs to be used on a network aswell


progression - scriptable objects with level requirements, levels and what levels unlock what,