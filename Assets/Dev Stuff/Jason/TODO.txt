TODO

ship - map display script(works with move script to change destination), 

gravity - do a mesh gravity thing, gravity plane but with an underneath too.

character - Object Placement scripts, finish character values, finish UI, Ik handler(George's tool), animation handler(dont really need this tho, we can just feed the animator the values of movement to use in a blendtree)
// note: make sure to keep in mind that this needs to be used on a network aswell

inventory - drop item(need custom grav script), rotation, durability, fix bug where inventory with no items inside place new items outside of grid, hotbar

progression - scriptable objects with level requirements, levels and what levels unlock what,

wire - wraps around object, dont use another script determine viable obstacles via rays


JOHN BERKLEY